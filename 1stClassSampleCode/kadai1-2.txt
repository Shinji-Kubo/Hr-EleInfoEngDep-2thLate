#include<stdio.h>
#define N 100

main(void) {
	int i;
	int num;
	int factor[N];
	int cnt = 0;

	printf("数値を入力してください : ");
	scanf("%d", &num);

	for (i=2; i<=num; i++) {
		while (num % i == 0) {
			factor[cnt] = i;
			cnt++;
			num /= i;		
		}
	}

	for (i=0; i<cnt; i++) {
		printf("%d ", factor[i]);
	}

	printf("\n");

	return 0;
}

数値を入力してください : 16
2 2 2 2 
数値を入力してください : 15
3 5 
数値を入力してください : 54
2 3 3 3 
数値を入力してください : 987654
2 3 97 1697 
